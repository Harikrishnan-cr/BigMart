



import 'package:flutter/material.dart';

class DividerOne extends StatelessWidget {
  const DividerOne({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Divider(
      thickness: 1,
    );
  }
}